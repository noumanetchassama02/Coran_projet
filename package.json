{
  "name": "coran-app",
  "version": "1.0.0",
  "description": "Application Coran — lecture, écoute et mémorisation. Web (PWA) + Android (APK/AAB via Capacitor).",
  "private": true,
  "scripts": {
    "cap:add": "cap add android",
    "cap:sync": "cap sync",
    "cap:open": "cap open android",
    "build:apk": "cap sync android && cd android && gradlew.bat assembleDebug",
    "build:aab": "cap sync android && cd android && gradlew.bat bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
